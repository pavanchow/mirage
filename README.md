# Mirage

A virtual machine that executes its own bytecode, covering the instruction set, the stack, and a simple compiler front end.

## Status

Early stage. Project scaffolding only, implementation not started yet.
